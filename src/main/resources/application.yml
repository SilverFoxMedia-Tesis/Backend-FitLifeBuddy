spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitLifeBuddy
    username: postgres
    password: joshue123
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true

